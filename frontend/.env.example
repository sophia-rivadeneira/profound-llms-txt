# Copy this file to .env.local and edit the values for your local environment.
# .env.local is gitignored.

# Backend API base URL — used in the browser for all fetch calls.
NEXT_PUBLIC_API_URL=http://localhost:8000
